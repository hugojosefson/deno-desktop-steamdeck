{
  "tasks": {
    "dev": "deno desktop main.ts",
    "build": "deno desktop --output hello.AppImage --target x86_64-unknown-linux-gnu main.ts",
    "all": "deno fmt --check && deno lint && deno check main.ts"
  }
}
